# Google Cloud Vision API credentials path
# Uncomment and set one of these options:

# Option 1: Path to your service account key file
GOOGLE_APPLICATION_CREDENTIALS=./key.json

# Option 2: Alternative way to specify key file
# GOOGLE_CLOUD_KEYFILE=./key.json

# Server port
PORT=8080

# Note: For local development without real credentials,
# the app will use a mock client that returns sample data
